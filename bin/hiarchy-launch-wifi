#!/bin/bash

# Launch the Hiarchy wifi controls (provided by the Gazelle TUI).
# Attempts to unblock the wifi service first in case it should be been blocked.

rfkill unblock wifi
hiarchy-launch-or-focus-tui gazelle
