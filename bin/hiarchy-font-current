#!/bin/bash

# Returns the name of the current monospace font being used by extracting it from the Waybar stylesheet.
# This can be changed using hiarchy-font-set.

grep -oP 'font-family:\s*["'\'']?\K[^;"'\'']+' "$HIARCHY_PATH/default/waybar/style.css" | head -n1
