#!/bin/bash

# Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/hiarchy/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  hiarchy-theme-set "$(cat $THEME_NAME_PATH)"
fi
