#!/bin/bash

# Overwrite the user configs for swayosd (controls on-screen feedback for changing volume/songs etc) with the Hiarchy defaults and restart the service.

hiarchy-refresh-config swayosd
hiarchy-restart-swayosd
