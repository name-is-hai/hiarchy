#!/bin/bash

# Remove preinstalled Hiarchy applications (web apps, TUIs, and selected packages).
# This removes all web apps, TUIs, plus specific desktop applications.

if gum confirm "Are you sure you want to remove all preinstalled web apps, TUI wrappers, and desktop applications?"; then
  echo -e "Removing preinstalled Hiarchy applications...\n"

  hiarchy-webapp-remove-all
  hiarchy-tui-remove-all

  hiarchy-pkg-drop \
    aether \
    libreoffice-fresh \
    xournalpp \
    signal-desktop \
    pinta \
    obsidian \
    obs-studio \
    kdenlive
fi
